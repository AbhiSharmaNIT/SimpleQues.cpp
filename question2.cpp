#include <iostream>
#include <algorithm>
#include <math.h>
#include <vector>
#include <cmath>
#include<cstring>
using namespace std;

int main()
{
    
    int arr[n];
    for (int i = 0; i < n; i++)
    {
        cin>>arr[i];
    }
    
    int num1 = arr[n-1];
    int num2 = arr[n-2];

    string str1 = to_string(num1);
    string str2 = to_string(num2);

    cout<<"k"+str2+str1;

}